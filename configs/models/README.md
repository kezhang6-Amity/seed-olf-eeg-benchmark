# Model configurations

Add one reviewed configuration per new model family. Every configuration must state the source implementation, preprocessing, grouped validation protocol, tuning budget, random seeds, and comparator.
